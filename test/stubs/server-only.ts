/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when it is imported from a client bundle, which is
 * exactly what we want in the app and exactly what makes those modules
 * unimportable from Vitest. Aliased in vitest.config.ts.
 */
export {}
